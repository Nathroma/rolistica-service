export enum ItemType {
    weapon = 'weapon',
    armor = 'armor',
    tool = 'tool',
    other = 'other',
}

export enum ItemAction {
    action = 'action',
    bonusAction = 'bonusAction',
    reaction = 'reaction',
    other = 'other',
}
