export enum ItemType {
  WEAPON = 'weapon',
  ARMOR = 'armor',
  TOOL = 'tool',
  OTHER = 'other',
}

export enum ItemAction {
  ACTION = 'action',
  BONUS_ACTION = 'bonusAction',
  REACTION = 'reaction',
  OTHER = 'other',
}